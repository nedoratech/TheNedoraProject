/** Shared vertical rhythm — tighter than the original py-20/24/32 marketing scale */

export const SECTION_PY = 'py-14 sm:py-18 lg:py-24'

export const SECTION_HEADER_GRID =
  'grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 items-end'

export const SECTION_BLOCK_MT = 'mt-8 sm:mt-10 lg:mt-12'

/** Desktop body copy — 1rem (18px at site root). */
export const TEXT_BODY_DESKTOP = 'lg:text-[1rem]'

/** Desktop section labels + secondary card copy — 0.889rem (16px at site root). */
export const TEXT_16_DESKTOP = 'lg:text-[0.889rem]'

/** Footer — 0.889rem mobile (16px), 1rem desktop (18px at site root). */
export const TEXT_FOOTER = 'text-[0.889rem] lg:text-[1rem]'

/** Body copy line-height — paragraphs, lists, and secondary text. */
export const LEADING_BODY = 'leading-[1.5]'

/** Multi-line section headings (h2). */
export const LEADING_HEADING = 'leading-[1.05]'
